{
  "name": "tic-tac-toe-server", // Project name
  "version": "1.0.0", // Project version
  "main": "server.js", // Entry point file
  "type": "module", // Use ES modules (import/export syntax)
  "scripts": {
    "start": "node server.js", // Script to run the server
    "dev": "nodemon server.js" // Script to run the server in development mode with auto-reload
  },
  "dependencies": {
    "http": "^0.0.1-security", // Minimal http module (placeholder; not recommended for real HTTP handling)
    "socket.io": "^4.7.3" // WebSocket library for real-time communication
  },
  "devDependencies": {
    "nodemon": "^3.1.10" // Dev tool to auto-restart server on code changes
  }
}